#include <stdio.h>

int main() {
    int a, b, maior;

    printf("Digite o primeiro número: ");
    scanf("%d", &a);

    printf("Digite o segundo número: ");
    scanf("%d", &b);

    maior = (a > b) ? a : b;

    printf("O maior número é: %d\n", maior);

    return 0;
}